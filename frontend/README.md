# ExcipiHub frontend (React + Vite)

## Setup

    npm install
    npm run dev

Open http://localhost:5173 — the backend must be running at
http://127.0.0.1:8000 (see the backend's own README) for anything to load.
